# NextTex reads this when the project is opened.  Every field is optional;
# without the file NextTex guesses, and usually guesses right.
[project]
name = "Minimal Article"
main = "main.tex"
build_dir = "build"
